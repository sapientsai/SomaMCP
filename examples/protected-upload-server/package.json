{
  "name": "@somamcp-examples/protected-upload-server",
  "version": "0.0.0",
  "private": true,
  "description": "Example wiring httpStream + addRoute (POST) + authenticate + a tool — fixture for the protected-routes surface.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "somamcp": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "rimraf": "^6.1.3",
    "tsdown": "^0.21.10",
    "tsx": "^4.23.5",
    "typescript": "^5.9.3"
  }
}
